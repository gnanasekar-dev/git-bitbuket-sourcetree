I.---  origin, upstream   ==> userdefined
II.---  origin    ==> most common for your repo connection
III.---  upstream  ==> most common for others repo connection


***************************************************************

1.---  git clone https://github.com/gnanasekar-dev/git-bitbuket-sourcetree.git ( repo .git url )

2.---  git remote add origin https://github.com/gnanasekar-dev/git-bitbuket-sourcetree.git ( Read[fetch] + Write[push] )

3.---  git remote add upstream https://github.com/dompascal/gitium.git ( Read )

### See all the connected remote repo's
4.---  git remote

### See permissions fetch,push ( Read,Write )
5.---  git remote -v

### Will upload any branch to the remote origin ( https://github.com/gnanasekar-dev/git-bitbuket-sourcetree.git )
6.---  git push origin branchname

7.---  git pull origin branchname

### Can download any branches of any public repo but should make a connection to repo with [ git remote add upstream https://github.com/dompascal/gitium.git]
8.---  git pull upstream branchname


=====
To push the init repo to github

1. git remote add origin https://github.com/gnanasekar-dev/git-bitbuket-sourcetree.git
2. git push -u origin master
